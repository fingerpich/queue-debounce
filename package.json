{
  "name": "queue-debounce",
  "version": "0.2.0",
  "description": "queue and debounce  events",
  "repository": "fingerpich/queue-debounce",
  "main": "index.js",
  "scripts": {
    "build": "tsc ./index.ts",
    "dev": "ts-node-dev --respawn --transpile-only ./index.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.2.4",
    "@types/node": "^16.9.1"
  }
}
